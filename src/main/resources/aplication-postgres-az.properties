database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://spring-petclinic-postgres1.postgres.database.azure.com:5432/spring-petclinic-postgres1}
spring.datasource.username=${POSTGRES_USER:pcarfig}
spring.datasource.password=${POSTGRES_PASS:Admin.123}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always